services:
  security-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: security-api
    environment:
      CONFIG_PATH: /app/config.yaml
    ports:
      - "5000:5000"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./temp:/app/temp   # folder sementara untuk artifacts
