# Keep build context lean and avoid leaking secrets
.git
.gitignore
config.yaml
.env
*.log
temp
*.sqlite
*.db

# Dependency/artifact directories
vendor
node_modules
dist
bin

# Local notes and design docs. Gitignored, and no business in a build context.
docs

# Docker
docker-compose.yml
